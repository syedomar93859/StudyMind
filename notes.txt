important commands:
npm init -y
npm install express(npm i express)

node welcome.js

use this command to find the PID or the 3000 server:
lsof -i :3000

use this command to stop the server but replace 68191 with the PID:
kill -9 68191

command for gemini sdk:
npm install @google/genai

handling API keys safely:
npm install dotenv

you have to create an a gemini api key:
https://ai.google.dev/gemini-api/docs/quickstart

this is for converting from markdown to html:
npm install marked

Implement these later:
Kinda Done: Ask the user after they have decided to click the delete button for a note
Login system
Done: Make sure note titles are fully visible
Add settings to log out
Check if password is strong
Tell the user that the checkbox has not been checked
during registration prevent duplicate accounts
after clicking sign up on the registration page and the details enter are valid, go back to the login page
make sure notes are tied to accounts
passwords need to be encrypted
multiple same accounts cant exist
add a way to view the full note
maybe add a way to make the password visible when typing it
maybe a color indicator to show that the password is strong or not(red means weak and green means strong)
add terms and conditions
when changing from register form to login form to register form, maybe the username and email should be cleared